import React, { Fragment } from 'react';
import { Header } from "../Header";
import { Main } from "../Main";
import './style.css';

export const App = () => (
    <Fragment>
        <Header />
         <Main />
    </Fragment>
);
